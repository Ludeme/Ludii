package game.types.board;

/**
 * Defines the different stores for a mancala board.
 * 
 * @author Eric.Piette
 */
public enum StoreType
{
	/** No store. */
	None,

	/** Outer store. */
	Outer,

	/** Inner store. */
	Inner;
}
