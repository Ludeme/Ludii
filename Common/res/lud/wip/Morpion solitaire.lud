(game "Morpion Solitaire" 
    (players 1) 
    (equipment { 
    	(board 
                (square 20) 
                use:Vertex
            )
        (piece "Marker" P1 ("HopFriendCapture" Orthogonal))
    }) 
    (rules 
//        (start { 
//            (place "Marker" (difference (sites Board) (sites "D4")))
//        })
        
        (play (forEach Piece))
        (end (if (and (is Friend (who at:(centrePoint))) (= 1 (count Pieces All))) (result P1 Win))) 
    )
)

//------------------------------------------------------------------------------

(metadata 
    
    (info
        {
        }
    )
    
    (graphics {
        //(board Style Graph)
        (show Edges Orthogonal Hidden)
        (show Edges Diagonal Hidden)
        (board Colour OuterEdges (colour Hidden))
        (board Colour OuterVertices (colour Hidden))
        //(show Vertices Hidden)
        //(board StyleThickness InnerVertices 3.0)
    })
    
)

