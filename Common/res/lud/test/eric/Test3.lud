(define "SquarePattern" (is Pattern {F F R F F R F F R F F} whats:{(mover) 0 (mover) 0 (mover) 0 (mover) 0}))

//------------------------------------------------------------------------------

(game "Test3"
    (players 2) 
    
    (equipment { 
        (board (square 5))
        (hand Each)
        (piece "Ball" Each) 
    })  
    (rules 
        (play (move Add (to (sites Empty))))
        
        (end 
            (if 
                ("SquarePattern") 
                (result Mover Win)
            )
        )
    )
)