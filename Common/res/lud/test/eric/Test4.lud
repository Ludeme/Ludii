(define "SizeStackInPlaceStack"
    (place Stack "Disc1" 0 count:4)
    (place Stack "Disc1" 1 count:(size Stack at:0))
)

(game "Container ludemes in start rule"
    (players 2)  
    (equipment { 
        (board (hex 2)) 
        (piece "Disc" Each)
    })
    (rules
        (start {
            //Each of these causes java.lang.NullPointerExceptions with different trace
            //            (place Stack "Disc1" 0 count:(count Steps 0 6))
            // (place Stack "Disc1" 0 count:(count Sites in:(sites Board)))
            //("SizeStackInPlaceStack")
        })
        (play 
            (move Add (to (sites Empty)))
        )
        
        (end {
        })
    )
)
