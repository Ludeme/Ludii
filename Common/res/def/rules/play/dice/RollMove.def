// Roll dice before to compute the legal moves.
// #1 = The legal moves.
// #2 = The consequences.
// @example ("RollMove" (forEach Piece))

(define "RollMove"
    (do (roll) 
        next:#1
        #2
   )
)
