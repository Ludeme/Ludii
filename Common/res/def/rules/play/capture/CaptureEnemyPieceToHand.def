// Defines the capture of an enemy piece returning to his hand.
// @example ("CaptureEnemyPieceToHand")

(define "CaptureEnemyPieceToHand"
    (apply 
        if:("IsEnemyAt" (to)) 
        (fromTo (from (to)) (to (handSite (who at:(to)))))
    ) 
)
