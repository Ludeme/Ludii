// Defines a custodial capture in all the directions specified (by default Adjacent). The enemy piece is removed.
// #1 = Directions of the capture.
// #2 = The consequences.
// @example ("CustodialCapture")

(define "CustodialCapture"
    (custodial 
        (from (last To))
        #1
        (between 
            if:("IsEnemyAt" (between))
            (apply (remove (between)))
        )
        (to if:("IsFriendAt" (to)))
        #2
    )
)
