(define "Challis Ghutia (Titagarh)_ai"
    (useFor { "Start Rules/24 pieces" }
        (bestAgent "AlphaBeta")
        trainedHeuristics:(heuristics {
            (material)
        })
        
    )
)
