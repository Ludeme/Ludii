(define "Rimau-Rimau (Two Tigers)_ai"
    (useFor { "Start Rules/Tigers Start in hand" }
        (bestAgent "AlphaBeta")
        trainedHeuristics:(heuristics {
            (material)
        })
        
    )
    (useFor { "Start Rules/Tigers start at the centre" }
        (bestAgent "AlphaBeta")
        trainedHeuristics:(heuristics {
            (material)
        })
        
    )
)
