(define "Dabuda_ai"
    (useFor { "Opening/One Rightmost hole" }
        (bestAgent "MAST")
        trainedHeuristics:(heuristics {
            (ownRegionsCount)
        })
        
    )
    (useFor { "Opening/Four Rightmost holes" }
        (bestAgent "MAST")
        trainedHeuristics:(heuristics {
            (ownRegionsCount)
        })
        
    )
)
