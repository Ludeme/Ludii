(define "Chaturanga_ai"
    (useFor { "Play Rules/Elephant Move is Orthogonal Jump" }
        (bestAgent "AlphaBeta")
        trainedHeuristics:(heuristics {
            (material)
        })
        
    )
    (useFor { "Play Rules/Elephant Move is Diagonal Jump" }
        (bestAgent "AlphaBeta")
        trainedHeuristics:(heuristics {
            (material)
        })
        
    )
    (useFor { "Play Rules/Elephant Move is like a Silver in Shogi" }
        (bestAgent "AlphaBeta")
        trainedHeuristics:(heuristics {
            (material)
        })
        
    )
)
